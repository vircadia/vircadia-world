{{- if .Values.stateManager.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vircadia-world.stateManager.fullname" . }}
  labels:
    {{- include "vircadia-world.labels" . | nindent 4 }}
    app.kubernetes.io/component: state-manager
spec:
  {{- if not .Values.stateManager.autoscaling.enabled }}
  replicas: {{ .Values.stateManager.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "vircadia-world.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: state-manager
  template:
    metadata:
      labels:
        {{- include "vircadia-world.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: state-manager
    spec:
      serviceAccountName: {{ include "vircadia-world.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: state-manager
          image: "{{ .Values.stateManager.image.repository }}:{{ .Values.stateManager.image.tag }}"
          imagePullPolicy: {{ .Values.stateManager.image.pullPolicy }}
          command: ["bun", "run", "dist/world.state.manager.js"]
          ports:
            - name: http
              containerPort: {{ .Values.stateManager.service.targetPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "vircadia-world.fullname" . }}-config
            - secretRef:
                name: {{ include "vircadia-world.fullname" . }}-secret
          livenessProbe:
            httpGet:
              path: /stats
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /stats
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.stateManager.resources | nindent 12 }}
          volumeMounts:
            - name: app-code
              mountPath: /app
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: app-code
          configMap:
            name: {{ include "vircadia-world.fullname" . }}-state-code
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }} 